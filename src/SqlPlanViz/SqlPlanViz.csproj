<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <TargetPlatformMinVersion>10.0.17763.0</TargetPlatformMinVersion>
    <SupportedOSPlatformVersion>10.0.17763.0</SupportedOSPlatformVersion>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <UseWinUI>true</UseWinUI>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <AssemblyName>SqlPlanViz</AssemblyName>
    <RootNamespace>SqlPlanViz</RootNamespace>
    <ApplicationIcon></ApplicationIcon>
  </PropertyGroup>

  <!--
    §5 packaging: unpackaged + self-contained. The Windows App SDK runtime ships inside
    the output folder, so there is no runtime install / installer dependency chain.
    (PublishSingleFile is not supported by WinUI 3 — a self-contained folder is the
    equivalent "just run it" deliverable. See README.)
  -->
  <PropertyGroup>
    <WindowsPackageType>None</WindowsPackageType>
    <WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>
    <SelfContained>true</SelfContained>
    <EnableMsixTooling>true</EnableMsixTooling>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.6.250228001" />
    <PackageReference Include="Microsoft.Graphics.Win2D" Version="1.3.2" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.2.2" />
    <PackageReference Include="Microsoft.Data.SqlClient" Version="6.1.2" />
    <!-- Explicit reference for the custom InteractiveAuthProvider; version matches the one
         Azure.Identity 1.14.2 already brings in transitively via Microsoft.Data.SqlClient. -->
    <PackageReference Include="Microsoft.Identity.Client" Version="4.73.1" />
    <!-- The parser SSMS and SqlPackage use. Powers editor highlighting, completion context,
         parameter discovery and the batch safety analyzer (docs/live-plan-editor-plan.md). -->
    <PackageReference Include="Microsoft.SqlServer.TransactSql.ScriptDom" Version="180.78.1" />
  </ItemGroup>

</Project>
